import { Component, Input } from '@angular/core';

@Component({
    selector: 'app-likedButton',
    templateUrl: './likedButton.component.html',
    styleUrls: ['./likedButton.component.scss']
})

export class LikedButtonComponent{

    async onClick(){
       
    }
}